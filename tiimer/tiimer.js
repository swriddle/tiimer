function checkStartButtonStatus() {
    var expr_element = document.getElementById("expr");
    var content = expr_element.value;
    console.log("content = '" + content + "'");
    parse(content);
}

function parseTime(string) {
    const timeRegex = /^(?:(\d)+h)?(?:(\d)+m)?(?:(\d)+s)?/;
    let m = timeRegex.match(string);
    if
	// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Regular_Expressions(m) {
    }
    
	
}

function parse(string) {
    
    a
}
